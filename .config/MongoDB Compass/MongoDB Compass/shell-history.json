["clear()","clear","users = db.usuarios.find( { email: /lucas.almeida/ } );\nusers.forEach(user => {\n  db.usuarios.updateOne(\n    { _id: user._id },\n    { $set: { nome: user.nome.replace('Lucas', 'Pedro') } }\n  );\n});\nusers = db.usuarios.find( { email: /lucas.almeida/ } );\nusers.forEach(user => {\n  db.usuarios.updateOne(\n    { _id: user._id },\n    { $set: { email: user.email.replace('lucas.almeida', 'pedro.branquinho')  } }\n  );\n});\nusers = db.usuarios.find( { email: /pedro.branquinho/ } );\nusers.forEach(user => {\n  db.usuarios.updateOne(\n    { _id: user._id },\n    { $set: { senha: '$2a$10$5d3YBFDYlbguF0njwIzto.8peeIFwkpxAoa0C6BMRdsktu9nQIbai' } }\n  );\n});\n","const users = db.usuarios.find( { email: /lucas.almeida/ } );\nusers.forEach(user => {\n  db.usuarios.updateOne(\n    { _id: user._id },\n    { $set: { nome: user.nome.replace('Lucas', 'Pedro') } }\n  );\n});\nconst users = db.usuarios.find( { email: /lucas.almeida/ } );\nusers.forEach(user => {\n  db.usuarios.updateOne(\n    { _id: user._id },\n    { $set: { email: user.email.replace('lucas.almeida', 'pedro.branquinho')  } }\n  );\n});\nconst users = db.usuarios.find( { email: /pedro.branquinho/ } );\nusers.forEach(user => {\n  db.usuarios.updateOne(\n    { _id: user._id },\n    { $set: { senha: '$2a$10$5d3YBFDYlbguF0njwIzto.8peeIFwkpxAoa0C6BMRdsktu9nQIbai' } }\n  );\n});","use gestao-ppi-dev"]